---
# Install and configure Syncthing
- name: Install Syncthing
  block:
    - name: Install Syncthing (MacOS)
      community.general.homebrew_cask:
        name: syncthing
        state: present
      when: is_macos
      register: syncthing_macos_install
      
    - name: Install Syncthing (Linux)
      when: is_linux
      become: true
      block:
        - name: Add Syncthing repository key
          apt_key:
            url: https://syncthing.net/release-key.txt
            state: present
            id: 37C84554E7E0A261E4F76E1ED26E6ED000654A3E
          when: is_wsl
          register: syncthing_key_add
          
        - name: Add Syncthing repository
          apt_repository:
            repo: deb https://apt.syncthing.net/ syncthing stable
            state: present
            filename: syncthing
          when: is_wsl
          register: syncthing_repo_add
          
        - name: Install Syncthing package
          apt:
            name: syncthing
            state: present
            update_cache: yes
          register: syncthing_linux_install
  rescue:
    - name: Handle Syncthing installation failure
      debug:
        msg: "Failed to install Syncthing. Please check system requirements and try again."
  tags: ['syncthing']

# Create required directories
- name: Create Syncthing directories
  file:
    path: "{{ item | expanduser }}"
    state: directory
    mode: '0700'
  with_items:
    - "~/.config/syncthing"
  tags: ['syncthing']

# Configure Syncthing
- name: Update secrets file with Syncthing configuration
  blockinfile:
    path: "{{ secrets_file }}"
    create: yes
    mode: '0600'
    block: |
      syncthing_api_key: "{{ syncthing_api_key | default(lookup('password', '/dev/null chars=ascii_letters,digits length=32')) }}"
      syncthing_secrets_folder_id: "{{ syncthing_secrets_folder_id }}"
      syncthing_scripts_folder_id: "{{ syncthing_scripts_folder_id }}"
      syncthing_ssh_folder_id: "{{ syncthing_ssh_folder_id }}"
  tags: ['syncthing']

- name: Configure Syncthing
  block:
    - name: Validate Syncthing configuration
      template:
        src: syncthing_config.xml.j2
        dest: "{{ ansible_user_dir }}/.ansible/tmp/ansible-tmp-{{ ansible_date_time.epoch }}-{{ ansible_pid }}-{{ ansible_unique_id }}/.source.xml"
        mode: '0600'
      register: syncthing_config_template

    - name: Configure Syncthing
      command: >
        /usr/local/bin/syncthing cli config validate {{ ansible_user_dir }}/.ansible/tmp/ansible-tmp-{{ ansible_date_time.epoch }}-{{ ansible_pid }}-{{ ansible_unique_id }}/.source.xml
      register: syncthing_config_validate
      when: is_macos
      failed_when: syncthing_config_validate.rc != 0
      changed_when: false

    - name: Apply Syncthing configuration
      command: >
        /usr/local/bin/syncthing cli config update {{ ansible_user_dir }}/.ansible/tmp/ansible-tmp-{{ ansible_date_time.epoch }}-{{ ansible_pid }}-{{ ansible_unique_id }}/.source.xml
      register: syncthing_config_update
      when: is_macos and syncthing_config_validate is success
      failed_when: syncthing_config_update.rc != 0

    - name: Configure Syncthing
      template:
        src: syncthing_config.xml.j2
        dest: ~/.config/syncthing/config.xml
        mode: '0600'
        validate: 'syncthing cli config validate %s'
      register: syncthing_config
      when: not is_macos
  tags: ['syncthing']

# Service Management
- name: Start Syncthing service (Linux)
  systemd:
    name: syncthing@{{ ansible_user_id }}
    state: started
    enabled: yes
  become: true
  when: is_linux
  tags: ['syncthing']

- name: Start Syncthing service (macOS)
  command: brew services start syncthing
  register: syncthing_service_start
  changed_when: "'Successfully started' in syncthing_service_start.stdout"
  failed_when: syncthing_service_start.rc != 0 and 'already started' not in syncthing_service_start.stderr
  when: is_macos
  tags: ['syncthing']

- name: Wait for Syncthing service
  wait_for:
    port: 8384
    timeout: 30
  when: syncthing_service_start is changed
  tags: ['syncthing']

# Verify Installation
- name: Verify Syncthing is running
  command: pgrep syncthing
  register: syncthing_check
  changed_when: false
  retries: 3
  delay: 5
  until: syncthing_check.rc == 0
  tags: ['syncthing']
